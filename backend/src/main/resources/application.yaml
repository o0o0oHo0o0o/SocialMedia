spring:
  application:
    name: SocialMedia
  datasource:
    url: jdbc:sqlserver://localhost:1434;databaseName=SocialMedia;encrypt=true;trustServerCertificate=true
    username: sa
    password: SocialMedia@2025
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  security:
    user:
      name: sa
      password: SocialMedia@2025
  data:
    redis:
      host: localhost
      port: 6379
      password: SocialMedia@2025
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
        shutdown-timeout: 2000ms
jwt:
  secret: f9d18da1b0fe4a035818eeb2a46da7ab2741c5f0ee7e978505137520ba8a61
  expiration: ${JWT_EXPIRATION:86400000}
security:
  cors:
    allowed-origins:
      - http://localhost:3000
      - http://localhost:4200
      - ${FRONTEND_URL:http://localhost:8080}
    allowed-methods:
      - GET
      - POST
      - PUT
      - DELETE
      - PATCH
      - OPTIONS
    allowed-headers:
      - Authorization
      - Content-Type
      - X-Requested-With
      - Accept
    exposed-headers:
      - Authorization
      - X-Token-Expired

